name: ict602_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# TAMBAH INI DI BAWAH publish_to
environment:
  sdk: '>=3.0.0 <4.0.0'  # atau '>=3.10.0 <4.0.0'

version: 1.0.0+1

dependencies:
  flutter:
    sdk: flutter
  # Firebase Core (wajib)
  firebase_core: ^2.24.2
  # Firebase Auth
  firebase_auth: ^4.12.0
  # Cloud Firestore
  cloud_firestore: ^4.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
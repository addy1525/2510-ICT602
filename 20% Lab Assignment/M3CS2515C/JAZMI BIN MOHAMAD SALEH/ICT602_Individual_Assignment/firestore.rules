rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ❗ DEVELOPMENT ONLY: buka semua akses
      allow read, write: if true;
    }
  }
}
